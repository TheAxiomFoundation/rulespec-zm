# Known validation gaps ratchet for rulespec-zm.
#
# Exact combined validator + independently executed companion failures,
# keyed by module path. Entries are a floor to burn down, never to grow;
# the supervised waiver audit enforces the schema and the decrement-only
# ratchet. The repo currently has no waived failures.
validate_failures: {}
